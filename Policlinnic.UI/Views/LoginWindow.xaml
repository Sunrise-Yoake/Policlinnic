<Window x:Class="Policlinnic.UI.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход в систему" Height="480" Width="380"
        WindowStartupLocation="CenterScreen" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border Background="White" CornerRadius="15" BorderBrush="#2E7D32" BorderThickness="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#2E7D32" CornerRadius="13,13,0,0">
                <Grid>
                    <TextBlock Text="АВТОРИЗАЦИЯ" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="15,0" FontWeight="Bold"/>
                    <Button Content="✕" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0" Foreground="White" Click="CloseButton_Click" Margin="0,0,10,0" Cursor="Hand" FontSize="16"/>
                </Grid>
            </Border>

            <StackPanel Grid.Row="1" Margin="35,20">
                <Grid Width="70" Height="70" Margin="0,20,0,15">
                    <Ellipse Fill="#2E7D32"/>
                    <Rectangle Fill="White" Width="35" Height="10" RadiusX="2" RadiusY="2"/>
                    <Rectangle Fill="White" Width="10" Height="35" RadiusX="2" RadiusY="2"/>
                </Grid>

                <TextBlock Text="ПОЛИКЛИНИКА" FontSize="26" FontWeight="ExtraBold" Foreground="#2E7D32" HorizontalAlignment="Center" Margin="0,0,0,40"/>

                <TextBlock Text="Логин" Foreground="#757575" Margin="0,0,0,5"/>
                <TextBox x:Name="LoginBox" Height="40" VerticalContentAlignment="Center" Padding="10,0" Margin="0,0,0,15"/>

                <TextBlock Text="Пароль" Foreground="#757575" Margin="0,0,0,5"/>
                <PasswordBox x:Name="PassBox" Height="40" VerticalContentAlignment="Center" Padding="10,0" Margin="0,0,0,30"/>

                <Button Content="ВХОД" Height="50" Background="#2E7D32" Foreground="White" FontWeight="Bold" FontSize="16" Click="LoginButton_Click">
                    <Button.Resources>
                        <Style TargetType="Border"><Setter Property="CornerRadius" Value="25"/></Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>