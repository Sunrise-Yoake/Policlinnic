<Page x:Class="Policlinnic.UI.Views.Pages.SickLeavesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="Больничные" Background="White" FontFamily="Segoe UI">

    <Page.Resources>
        <Style x:Key="GridActionButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="4,0"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F5F5F5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AddButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2E7D32"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#388E3C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FilterCombo" TargetType="ComboBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="BorderBrush" Value="#CFD8DC"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#EEEEEE"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsOpen}" Value="True">
                    <Setter Property="Background" Value="#10FF0000"/>
                </DataTrigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F1F8E9"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#C8E6C9"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}" BorderThickness="0">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NonFocusableCellStyle" TargetType="DataGridCell" BasedOn="{StaticResource {x:Type DataGridCell}}">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="IsHitTestVisible" Value="False"/>
        </Style>

    </Page.Resources>

    <Grid Margin="20" KeyboardNavigation.TabNavigation="Cycle">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <WrapPanel Grid.Column="0" Orientation="Horizontal">
                <StackPanel x:Name="PanelSpecFilter" Margin="0,0,15,0">
                    <TextBlock Text="Специализация" FontSize="11" Foreground="#90A4AE" Margin="0,0,0,4"/>
                    <ComboBox x:Name="CmbSpec" Width="180" Style="{StaticResource FilterCombo}"
                              SelectionChanged="Filters_SelectionChanged"/>
                </StackPanel>

                <StackPanel x:Name="PanelDoctorFilter" Margin="0,0,15,0" Visibility="Collapsed">
                    <TextBlock Text="Врач" FontSize="11" Foreground="#90A4AE" Margin="0,0,0,4"/>
                    <ComboBox x:Name="CmbDoctor" Width="180" Style="{StaticResource FilterCombo}"
                              SelectionChanged="Filters_SelectionChanged"/>
                </StackPanel>

                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Статус" FontSize="11" Foreground="#90A4AE" Margin="0,0,0,4"/>
                    <ComboBox x:Name="CmbStatus" Width="150" Style="{StaticResource FilterCombo}"
                              SelectionChanged="Filters_SelectionChanged">
                        <ComboBoxItem Content="Все" IsSelected="True"/>
                        <ComboBoxItem Content="Незакрытые (Открыт)"/>
                        <ComboBoxItem Content="Закрытые"/>
                    </ComboBox>
                </StackPanel>
            </WrapPanel>

            <Button x:Name="BtnAdd" Grid.Column="1" Content="+ Новый больничный" 
                Style="{StaticResource AddButtonStyle}" 
                VerticalAlignment="Bottom"
                Click="BtnAdd_Click"/>
        </Grid>

        <DataGrid x:Name="GridSickLeaves" Grid.Row="1" 
                  AutoGenerateColumns="False" IsReadOnly="True" 
                  CanUserAddRows="False" 
                  CanUserSortColumns="True"
                  GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#EEEEEE"
                  Background="White" BorderThickness="1" BorderBrush="#EEEEEE"
                  RowHeight="45" HeadersVisibility="Column"
                  SelectionMode="Single" SelectionUnit="FullRow">

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#F9FAFB"/>
                    <Setter Property="Foreground" Value="#607D8B"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="10,12"/>
                    <Setter Property="BorderThickness" Value="0,0,0,2"/>
                    <Setter Property="BorderBrush" Value="#EEEEEE"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Пациент" Binding="{Binding PatientFIO}" Width="*" 
                                    CellStyle="{StaticResource NonFocusableCellStyle}"/>

                <DataGridTextColumn x:Name="ColDoctor" Header="Врач" Binding="{Binding DoctorFIO}" Width="*" 
                                    CellStyle="{StaticResource NonFocusableCellStyle}"/>

                <DataGridTextColumn Header="Начало" Binding="{Binding DateStart}" Width="110" 
                                    CellStyle="{StaticResource NonFocusableCellStyle}"/>

                <DataGridTextColumn Header="Окончание" Binding="{Binding DateEnd}" Width="110" FontWeight="Bold" 
                                    CellStyle="{StaticResource NonFocusableCellStyle}"/>

                <DataGridTemplateColumn x:Name="ColActions" Width="100" Header="Действия">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="✏️" Style="{StaticResource GridActionButton}" Click="BtnEdit_Click" ToolTip="Редактировать"/>
                                <Button Content="✕" Style="{StaticResource GridActionButton}" Foreground="Red" Click="BtnDelete_Click" ToolTip="Удалить"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>