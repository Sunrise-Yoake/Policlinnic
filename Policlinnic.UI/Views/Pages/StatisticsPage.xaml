<Page x:Class="Policlinnic.UI.Views.Pages.StatisticsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="Статистика" Background="White" FontFamily="Segoe UI">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="33*"/>
            <RowDefinition Height="346*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="#EEEEEE" BorderThickness="0,0,0,1" Padding="0,0,0,20" Margin="0,0,0,120" Grid.RowSpan="2">
            <StackPanel Orientation="Horizontal">
                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Начало периода" Foreground="Gray"/>
                    <DatePicker x:Name="DpStart" Width="150" SelectedDateChanged="Date_Changed"/>
                </StackPanel>

                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Конец периода" Foreground="Gray"/>
                    <DatePicker x:Name="DpEnd" Width="150" SelectedDateChanged="Date_Changed"/>
                </StackPanel>

                <Button Content="Построить график" Width="150" Height="30" VerticalAlignment="Bottom"
                        Background="#2E7D32" Foreground="White" BorderThickness="0" Cursor="Hand"
                        Click="BtnBuild_Click"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="1" Margin="0,20,0,20">
            <TextBlock Text="Анализ тренда заболеваемости:" FontSize="14" FontWeight="Bold" Foreground="#455A64"/>
            <Border x:Name="TrendBorder" Background="#F5F5F5" Padding="15" CornerRadius="4" Margin="0,5,0,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="TxtTrendIcon" Text="❓" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="TxtTrendResult" Text="Выберите период для анализа" FontSize="16" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <Grid Grid.Row="2" Margin="0,20,0,0" Grid.RowSpan="2">
            <lvc:CartesianChart x:Name="ChartDiseases" LegendLocation="Bottom" HorizontalAlignment="Left" Width="960">
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis Title="Месяц" Labels="{Binding DateLabels}"/>
                </lvc:CartesianChart.AxisX>
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis Title="Количество диагнозов" MinValue="0" LabelFormatter="{Binding IntFormatter}">
                        <lvc:Axis.Separator>
                            <lvc:Separator Step="1"/>
                        </lvc:Axis.Separator>
                    </lvc:Axis>
                </lvc:CartesianChart.AxisY>
            </lvc:CartesianChart>
        </Grid>
    </Grid>
</Page>